"""Evaluation harness: pure metrics (`metrics`), dataset loading + a deterministic
synthetic generator (`dataset`), and a CLI runner (`runner`). This is what makes the
system verifiable — accuracy is measured, not asserted."""
